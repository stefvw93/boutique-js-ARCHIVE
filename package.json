{
  "name": "boutique",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "serve": "npm run build && vite preview"
  },
  "devDependencies": {
    "ts-essentials": "^8.0.0",
    "typescript": "^4.3.2",
    "vite": "^2.4.4"
  }
}
